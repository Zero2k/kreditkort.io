input SearchLoanInput {
  amount: String
  term: String
  loan_type: String
  filter: Filter
}

input Filter {
  ga: Boolean
  uc: Boolean
  ki: Boolean
}

type Query {
  searchLoan(
    input: SearchLoanInput
    offset: Int
    limit: Int
  ): [Loan!]!
}
