input SearchLoanInput {
  amount: Int
  interest: Int
  loan_type: String
  check_uc: Boolean
  bad_credit: Boolean
}

type Query {
  searchLoan(
    input: SearchLoanInput
    offset: Int
    limit: Int
  ): [Loan!]!
}
