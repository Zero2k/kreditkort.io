type Company {
  id: ID!
  name: String
  slug: String
  logo: String
  website: String
  about: String
  markdown: String
  creditcards(
    offset: Int
    limit: Int
  ): [Creditcard]!
  loans(
    offset: Int
    limit: Int
  ): [Loan]!
}
